# ERRORS.md — 错误模式记录

> 记录每次工具执行失败的根本原因，避免重复踩坑。
> 格式：日期、错误描述、根本原因、解决方案、预防措施

---

## 2026-04-05：错误模式——看图总用智谱/image tool
- **症状**：新 session 推荐智谱 glm-4.6v-flashx；尝试用 image tool 看图；老板说了100遍还是犯
- **根本原因**：MEMORY.md 旧配置没更新到位，习惯路径依赖；每次都先入为主去搜索 imageModel 配置而不是直接调 API
- **解决**：MEMORY.md 看图部分已改为 qwen-vl-plus（千问 DashScope）；彻底不用智谱
- **预防**：老板说换模型 → 立刻改 MEMORY.md，不拖延；看图只按 MEMORY.md 流程走（curl → qwen-vl-plus）

---

## 2026-04-05：错误模式——误报智谱 API 过期
- **症状**：告诉老板智谱额度到期了（其实是误判）
- **根本原因**：API 返回异常时没有仔细看错误信息，直接凭感觉判断为过期
- **解决**：确认 API Key 实际有效，换用 qwen-vl-plus
- **预防**：API 返回异常 → 先单独测试 API 是否真的过期，再告诉老板；不要在没确认前下结论

---

## 2026-04-04：错误模式——cron 任务 timeout 超时（automemory 等）
- **症状**：automemory cron 任务（b48d7143）连续 3 次 timeout；inbox清理（43880f68）连续 3 次 timeout；每周记忆清理（d5e15105）1 次 timeout
- **根本原因**：`agents.defaults.timeoutSeconds` 未显式设置，isolated subagent 用默认 ~90 秒，oMLX 分析超时会话需要更长时间
- **解决**：老板暂时决定不改（等有空再说），automemory 已手动禁用
- **预防**：isolated agent 任务需要较长超时，建议老板批准在 config 里加 `timeoutSeconds: 300`；同时优化脚本本身减少处理时间

---

## 2026-04-04：错误模式——误以为"配置删了模型就不占内存"
- **症状**：老板问"qwen3.5 模型不挂载是不是不占内存"，我说"是"，但没说完整
- **根本原因**：没说清楚关键是"服务停了才释放"，配置删除不等于模型卸载
- **解决**：告知必须停掉 oMLX 服务（`pkill -f omlx`）才真正释放内存
- **预防**：涉及资源占用问题时，要明确说清楚"服务运行状态"和"配置"是两回事

---

## 2026-04-02：错误模式——parseEmbeddedJson 被日志干扰
- **症状**：`openclaw memory status --json` 的 JSON 解析失败，Control UI 记忆状态显示错误
- **根本原因**：插件日志行 `[plugins] [lcm] Ignoring...` 里有 `[` 字符，被 `parseEmbeddedJson` 当作 JSON 开始
- **解决**：按行扫描 JSON，找到同行只有单个 `[` 或 `{` 的行作为 JSON 开始
- **预防**：输出格式解析时考虑日志混入的情况，不要假设输出只有纯 JSON

---

## 2026-04-02：错误模式——路径匹配不完整导致上传接口 404
- **症状**：HTML 调用 `/api/upload/send`，但 Python 服务器只处理 `/send`
- **根本原因**：路径匹配条件不完整，新增路径忘记更新
- **解决**：改用 `startswith` 前缀匹配
- **预防**：增加新路径时要同时检查所有调用点，确保前后端路径一致

---

## 2026-04-02：错误模式——误把计划中的事记成已完成
- **症状**：在内存 flush 里写"已更新 config"，但实际上还没改
- **根本原因**：把打算要做的事当成已经做的事记录
- **预防**：还没执行的操作只能记为"计划"，不能记为"已完成"

---

## 2026-04-01：错误模式——avatar 首字取法不对，三个都显示"小"
- **症状**：修复协作大厅头像 fallback，三个都显示"小"字
- **根本原因**：`charAt(0)` 取第一字，但小白/小红/小兰第一字都是"小"
- **解决**：改用 `slice(-1)` 取最后一个字
- **预防**：中文字符处理时要考虑重名情况，用 lastChar 而不是 firstChar

---

## 2026-04-01：错误模式——LaunchAgent kill 后自动重启导致进程冲突
- **症状**：kill upload_server.py 进程后，LaunchAgent 立刻拉起旧进程，新进程起不来
- **根本原因**：LaunchAgent 守护进程会自动重启已停止的服务
- **解决**：先 `launchctl stop` 停掉 LaunchAgent，再 kill 进程
- **预防**：涉及 LaunchAgent 管理的服务，先查清楚是否由 launchd 守护，再操作

---

## 2026-04-01：错误模式——在单个问题上周折太久不收手
- **症状**：图片分析接口全部失败，折腾了一晚上（23:08-23:15）
- **根本原因**：没有及时收手，换了 5+ 种方案逐一试，结果全失败
- **解决**：老板说"能停一下么？" → 立刻停，换方向
- **预防**：遇到复杂问题，15 分钟没进展就主动停下来汇报，不要闷头继续

---

## 2026-03-30：错误模式——exec 裸跑无 timeout 导致 gateway 彻底卡死
- **症状**：所有 exec 都无响应，连 `echo "test"` 都卡住，界面彻底冻结
- **根本原因**：MiniMax API 偶发性卡顿 + exec 没有 timeout 保护
- **解决**：重启 gateway 恢复；增加 AGENTS.md 规则：所有 exec 必须带 timeout
- **预防**：所有 exec 调用必须设 timeout，网络请求类 timeout=60，编译类 timeout=120+

---

## 2026-03-29：错误模式——向量搜索混用 Ollama 和 oMLX
- **症状**：之前同时配了两套向量搜索方案，配置混乱
- **根本原因**：没有统一规划，看到一个问题就加一套方案
- **解决**：删除 Ollama，统一走 oMLX（bge-m3-mlx-4bit）
- **预防**：同类工具只保留一套方案，不要重复建设

---

## 2026-04-06：错误模式——cron 任务全部 timeout（isolated agent 硬限制）
- **症状**：所有在 isolated 模式下运行的 cron 任务（inbox清理、记忆清理、每月review）全部 timeout，虽然 cron 配置了 timeoutSeconds: 600
- **根本原因**：OpenClaw isolated agent 模式有 ~70 秒硬上限，超时后自动截断，不管 timeoutSeconds 设多少
- **解决**：Python 脚本改用系统 crontab 直接调用（python3 /path/to/script.py），绕过 agent 隔离层
  - crontab 已添加：`0 0 * * *` inbox清理 + `0 3 * * 0` 记忆清理
  - cron 任务本身保留（备用），但实际执行靠系统 crontab
- **预防**：凡是需要跑 Python/Shell 脚本的定时任务，优先用系统 crontab；agent cron 只适合发消息类任务

---

## 2026-04-06：错误——虚假承诺"记下了"
- **事件**：老板昨天告诉我「新话题」= 结束+总结，我回复"记下了"但实际上只在当天memory文件里记了，**没有写进MEMORY.md主文件**
- **后果**：新session启动时只读MEMORY.md，约定丢失，老板今天重新教我
- **根因**：嘴上说记了，但没检查是否真的写进了核心记忆文件
- **纠正**：说"记下了"必须确认两件事：（1）写入了正确的文件（MEMORY.md或对应记忆文件）；（2）明确告诉老板"记下了"
- **额外问题**：老板还说我应该主动汇报"记下了"，而不是静悄悄写完
