

---

## 记忆补充 — 2026-04-17 00:02（第十二次 Pre-compaction Flush）

### 重大发现：昨晚 cron 实际成功了！
- 文件修改时间：Apr 15 21:35（昨晚8点cron触发后成功）
- 2026-04-15 数据：12905 条记录成功写入
- **昨晚的 isolated session（cron）没有被 SIGTERM kill，手动 exec 才会**

### 两个脚本的区别
- `astock_daily_update.py` = **日线全量更新**（从2021-04-01到今天）= cron 在用，**能跑完**
- `astock_today_update.py` = **今日增量**（只下今天单日数据）= 我手动 exec 在用，**一直 kill**

### 根因分析
- exec 非 isolated session：有约5分钟 SIGTERM 硬限制
- cron isolated session：**没有这个限制**，能跑完8分钟的脚本
- cron 的 "AUTONOMOUS: 运行 A 股每日更新"（ID: 50af4087）= isolated session，所以成功

### 手动触发方法（供老板参考）
老板如果想手动触发，可以用 `openclaw session spawn` 创建一个 isolated session 来跑脚本，绕过 SIGTERM 限制。

### 当前状态
- 4/16 A股数据：仍缺失（今天手动 exec 失败）
- 明天（4/17）cron：预计会成功（同 isolated session）
