# 2026-04-09 Daily Memory

## Session Start

### 老板反馈：Control UI 升级后空白问题
- 老板升级到 OpenClaw 4.9 后，Control UI 左侧菜单多个项目（Sessions、Usage、Cron、Memory 等）没有内容
- 原因分析：搜到 GitHub issue #41049（数据不自动加载，需手动 Refresh）、#53110（operator.read scope 问题导致 blanking）
- **解决方案：刷新一下页面就好**
- 教训：4.9 升级后 Control UI 首次打开需要手动刷新

### 老板要求：把 Obsidian 笔记导入梦境系统
- 老板想把 Obsidian Vault 的笔记导入 OpenClaw 4.9 的梦境（Dreaming）系统
- 发现 `openclaw memory` 相关命令：
  - `openclaw memory rem-harness --path <path> --grounded` — 预览历史 REM 输出
  - `openclaw memory rem-backfill --path <path>` — 写入 DREAMS.md
  - `openclaw memory rem-backfill --path <path> --stage-short-term` — 同时写入短term存储（可用于后续 promotion）
- **当前问题：memory-core 插件状态是 "unavailable"**，导致 dreaming 功能不可用
  - 原因：`openclaw.json` 中 `plugins.allow` 没有包含 `memory-core`
  - 老板说要先重启 gateway，但被中断了
  - **待办：重启 gateway，让 memory-core 加载，然后跑 rem-backfill**

### OpenClaw 4.9 更新内容（记忆）
- 新功能：Control UI 日记视图（structured diary view）
- 新功能：REM 回填功能（grounded historical backfill lane）
- 安全修复：SSRF 绕过、.env 隔离、节点 exec 注入
- 安全修复：QQBot 媒体标签 HTML 实体编码
- 插件配置：需把 `memory-core` 加入 `plugins.allow` 才能启用 dreaming

## OpenClaw 4.9 新增 allowlist 条目
老板说可以加，没问题：
```
"memory-core"
```

## 待办
- [ ] **重启 OpenClaw gateway** — 让 memory-core 插件真正加载
- [ ] 跑 `openclaw memory rem-backfill --path ~/Documents/Obsidian\ Vault/memory --stage-short-term` 导入 Obsidian memory/ 到梦境
- [ ] 根据老板需求，陆续导入其他 Obsidian 文件夹（memory-boss/、OpenClaw 日记/ 等）
- [ ] 把 MEMORY.md 中 "Obsidian memory 系统" 章节补充完整

## 记忆搜索策略确认（2026-04-09）
老板今天没有提出新的记忆搜索相关问题，继续用现有策略（grep 替代 memory_search）

## 小红/小兰协作
老板没有特别提到，继续保持
